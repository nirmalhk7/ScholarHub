<!DOCTYPE html>
<html>
<head>
  <title><%= WEB_DEFAULTS[:website_name] %>- <%= WEB_DEFAULTS[:website_full] %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <link href="https://fonts.googleapis.com/css?family=Hepta+Slab|Lobster&display=swap" rel="stylesheet">
  <style>
    .sk-three-bounce {
      margin: 40px auto;
      width: 80px;
      z-index: 9999;
      text-align: center; }
    .sk-three-bounce .sk-child {
      width: 20px;
      height: 20px;
      background-color: #333;
      border-radius: 100%;
      margin-right: auto;
      z-index: 9999;
      vertical-align: middle;
      display: inline-block;
      -webkit-animation: sk-three-bounce 1.4s ease-in-out 0s infinite both;
      animation: sk-three-bounce 1.4s ease-in-out 0s infinite both; }
    .sk-three-bounce .sk-bounce1 {
      -webkit-animation-delay: -0.32s;
      z-index: 9999;
      animation-delay: -0.32s; }
    .sk-three-bounce .sk-bounce2 {
      -webkit-animation-delay: -0.16s;
      z-index: 9999;
      animation-delay: -0.16s; }
    @-webkit-keyframes sk-three-bounce {
      0%, 80%, 100% {
        -webkit-transform: scale(0);
        transform: scale(0); }
      40% {
        -webkit-transform: scale(1);
        transform: scale(1); } }
    @keyframes sk-three-bounce {
      0%, 80%, 100% {
        -webkit-transform: scale(0);
        transform: scale(0); }
      40% {
        -webkit-transform: scale(1);
        transform: scale(1); } }
  </style>
</head>
<body>

<%= render :partial => "partials/loader" %>
<div class="app-container app-theme-white body-tabs-shadow fixed-sidebar fixed-header">

  <%= render :partial => "partials/navbar" %>
  <div class="app-main">

    <!-- NIRMAL SIDEBAR -->

      <div class="app-main__outer" style="padding-left: 0em!important;">
        <div class="app-main__inner" style="padding-left: 2em!important;">

          <%= yield %>

        </div>

        <%= render :partial => "partials/footer" %>
      </div>
  </div>
</div>
<script type="text/javascript" src="/assets/scripts/main.js"></script>
<script>
    $(window).load(function() {
        $('.loader').delay(100).fadeOut('slow');
    });
</script>
</body>
</html>
